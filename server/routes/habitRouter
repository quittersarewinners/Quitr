const path = require('path');
const express = require('express');
const router = express.Router();

const habitController = require('../controllers/habitController');

// router.get('/', habitController.someMiddleware, () =>{

// });

module.exports = router;
